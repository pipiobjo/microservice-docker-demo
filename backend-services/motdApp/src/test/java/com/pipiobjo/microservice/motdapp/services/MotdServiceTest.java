package com.pipiobjo.microservice.motdapp.services;

import org.junit.Test;

/**
 * GreetingService needs no spring context so simply test the service logic
 */
public class MotdServiceTest {

    @Test
    public void buildGreeting() {
//        String name = "john";
//        Motd motd = gs.buildGreeting(name);
//        assertThat(motd.getId()).isEqualTo(1);
//        assertThat(motd.getContent()).contains(name);
//
//        Motd second_motd = gs.buildGreeting(name);
//        assertThat(second_motd.getId()).isEqualTo(2);
//
//        Motd nullname_motd = gs.buildGreeting(null);
//        assertThat(nullname_motd.getContent()).contains(MotdService.GREETING_DEFAULT_VALUE);
    }
}